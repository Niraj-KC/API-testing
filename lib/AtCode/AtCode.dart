import 'package:flutter/material.dart';

class AtCode extends StatefulWidget {
  const AtCode({super.key});

  @override
  State<AtCode> createState() => _AtCodeState();
}

class _AtCodeState extends State<AtCode> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("At Code"),),

      body: Container(
        child: Column(
          children: [

          ],
        ),
      ),
    );
  }
}
